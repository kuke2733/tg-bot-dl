# 手动测试文档（TEST-GUIDE）

> 分支 `dev/20260912-features`，测试日期：2026-09-12。
> 本文档是 [TEST-PLAN.md](TEST-PLAN.md) 的可执行版本：每条用例给出具体操作步骤和预期现象，照着一步步做即可。
> 预期文案全部取自当前代码，如果机器人实际回复和文档写的不一样，就是问题，记录到文末的「问题记录」表。

## 测试前准备

### 1. 让分支代码生效

1. 网页面板（http://127.0.0.1:8080）点「停止」，再点「启动」。
2. 本机前台运行时直接 `python start.py`，日志就在控制台；Docker 跑用 `docker logs -f tg-bot-dl`。

### 2. 观察点（测试中随手核对）

| 位置 | 用途 |
| :--- | :--- |
| `data/` | 下载结果、频道文件夹、`.temp` 残留 |
| `config/queue.json` | 未完成任务落盘（`tasks` / `batches` 两个字段），任务结束对应条目应消失 |
| `config/listening.json` | 监听频道列表和广告过滤词库（`defaults.types` / `min_size_mb` / `ad_keywords`） |
| 控制台日志 | 恢复、假死、限流等只有日志能看到的行为 |

常用日志关键字：`New download initialized`（开始下载）、`恢复`（重启恢复）、`跳过疑似广告`（过滤）、`假死`（自愈）、`限流`（退避）。

### 3. 测试素材

- [ ] 大视频 2 个（每个 ≥ 几百 MiB，进度看得清）
- [ ] 相册一组（一次发 3 张图）
- [ ] 测试频道（公开或私有均可）：**用户账号已加入**（机器人不需要加入）；部署时已配置 `PHONE_NUMBER`
- [ ] （可选）>2GiB 的大文件一个

### 4. 一个重要前提

默认并发下载数是 6。**任务数 ≤ 6 时全部同时下载，看不到「排队中 ⏳」状态。** 要测排队相关用例（A3、A5、C1），先把面板环境变量 `MAX_CONCURRENT_DOWNLOADS` 设为 `1` 并重启；测完改回。

---

## A. 队列管理（/queue、/pause、/resume）

### A1 队列视图

操作：连发 3 个以上大文件（想看到 ⏳ 就按上面把并发调成 1），发 `/queue`。

预期：
- 标题 `📋 下载队列（共 N 个任务）`；
- 进行中的行以 `⬇️` 开头，排队的以 `⏳` 开头，后面是文件名（超长截断）和大小；
- 每行下方有两个按钮：`⏹ 文件名`（取消，原有功能不变）和 `📍 进度`（跳转）；
- 点 `📍 进度`：机器人在该任务的进度消息下面回一条定位消息（约 15 秒后自动删除），**点定位消息顶部的引用即可滚动到进度消息**；进度消息已被删除时弹「进度消息已失效」；
- 底部有 `🔄 刷新` 按钮，点击弹「已刷新」且数字更新。

结果：[✅] 通过　问题：＿＿＿

### A2 取消进行中的任务

操作：发一个大文件开始下载，点 /queue 里它对应的 `⏹` 按钮（或进度消息上的「停止」）。

预期：
- 按钮弹出提示「正在停止...」，随后任务从 /queue 列表消失；
- 原进度消息最终变成「已停止并删除」；
- `data/` 里没有对应的 `.temp` 残留。

结果：[✅] 通过　问题：＿＿＿

### A3 取消排队中的任务

操作：并发=1 时连发 2 个文件，等第一个开始后发 `/queue`，点排队任务（⏳）的 `⏹`。

预期：一秒内从列表消失；它原来那条「文件 `xxx` 已加入下载队列（大小）。」消息变成「已停止并删除」；第一个任务不受影响。

结果：[✅] 通过　问题：

### A4 文件夹（相册）任务取消

操作：发一组相册，然后 `/queue`，点批次对应按钮 `⏹ 文件夹名`。

预期：
- /queue 中显示 `📁 文件夹名（完成 x/y）`；
- 点「停止」后批次消息变成「已停止并删除」；
- `data/` 里该相册文件夹整个被清掉，不留文件。

结果：[✅] 通过　问题：

### A5 暂停 / 恢复

操作：并发=1，发 2~3 个文件，第一个下载中发 `/pause`，再发一个新文件；等一会发 `/queue`，再发 `/resume`。

预期：
- `/pause` 回复：「已暂停：排队中的任务不会开始，进行中的会继续下完。发 /resume 恢复。」；
- 进行中的任务下完，排队任务一直不动（/queue 数字不变）；
- `/queue` 标题行末尾带 `⏸ 已暂停`；
- `/resume` 回复：「已恢复，排队中的任务会继续下载。」，随后排队任务开始下载；
- 附带检查：暂停且队列空时，`/queue` 显示「⏸ 下载队列已暂停，发 /resume 恢复。」

结果：[✅] 通过　问题：＿＿＿

### A6 一键取消全部

操作：排 3 个以上任务（含下载中、排队、一个相册批次），发 `/queue`，点底部「🧹 全部取消」。

预期：
- 弹「已全部取消」，面板立刻刷新为「当前没有排队或进行中的下载任务。」；
- 排队任务的提示消息变为「已停止并删除」，下载中的任务停止后同样收尾；
- 相册批次消息变为「已停止并删除」，文件夹清空；
- `/queue` 底部按钮为「🔄 刷新」+「🧹 全部取消」两个。

结果：[✅] 通过　问题：＿＿＿

---

## B. 文件管理（/files）

### B1 浏览

操作：发 `/files`，点一个 `📁` 进入子目录，点 `⬅️ 上一级` 返回，点 `🔄 刷新`。

预期：
- 首页显示 `📂 当前目录：/` 和 `共 N 项：X 个目录，Y 个文件。`（空目录显示「这个目录是空的。」）；
- 目录/文件按钮两列排布，目录带 📁、文件带 📄；
- 进入子目录后路径更新，`⬅️ 上一级` 只在非根目录出现；
- 刷新弹「已刷新」。

结果：[✅] 通过　问题：＿＿＿

### B2 删除文件（含二次确认）

操作：点一个 `📄 文件` 按钮 → 看详情 → 点 `🗑 删除此文件` → 点 `❌ 取消` 先验证不删；再走一遍到 `✅ 确认删除`。

预期：
- 详情页显示文件名、`大小：…`、`修改时间：…`、`位置：/…`；
- 确认页显示「⚠️ 确认删除文件「文件名」？删除后无法恢复。」，带 `✅ 确认删除` / `❌ 取消` 两个按钮；
- 点取消回到列表，文件还在、磁盘上还在；
- 点确认弹「已删除」，列表刷新，磁盘上文件消失；去重记录同步清除——之后重发同一个文件按新文件处理，不会被误判「下载过」（批次停止清理掉的文件同样处理）。

结果：[✅] 通过　问题：＿＿＿

### B3 不能删除下载中的文件

操作：让一个大文件下载中，发 `/files`，找到它对应的 `.temp` 文件点删除并确认。

预期：弹出提示「这个文件正在下载中，请先取消下载。」，文件没有被删，下载继续。

结果：[✅] 通过　问题：＿＿＿

### B4 删除整个目录

操作：进入一个子目录，点导航行的「🗑 删除此目录」→ 确认删除；再对一个包含正在下载文件（.temp）的目录试删除。

预期：
- 导航行多出「🗑 删除此目录」按钮（仅子目录有，根目录没有）；
- 确认页「⚠️ 确认删除目录「X」？目录内全部内容将被删除，无法恢复。」；
- 确认后目录整个删除、回到上级列表，去重记录同步清理（重发其中的文件按新文件处理）；
- 目录内有正在下载的文件（.temp）时：自动取消占用它的下载任务、中止该频道的历史回填，然后照常删除（日志「删除目录前自动取消占用中的下载」）。

结果：[✅] 通过　问题：＿＿＿

---

## C. 队列持久化（重启恢复）

以下用例的「重启」= 面板「停止」→「启动」。

### C1 入队后重启

操作：并发=1，发 3 个文件（1 个下载中、2 个排队），确认 `config/queue.json` 的 `tasks` 里有 3 条，然后重启。

预期：
- 机器人上线后逐条出现「🔄 恢复下载任务 `文件名`。」，消息顶部带对原媒体消息的引用（点引用可跳回原文件消息），任务继续下载；
- `queue.json` 里任务条目随完成逐条减少，全部完成后清空。

结果：[✅] 通过　问题：＿＿＿

### C2 断点续传

操作：让大文件下载过半（进度消息超过 50%）再重启，恢复后观察进度起点，下载完打开文件。

预期：恢复后进度从已下载的大小附近继续（第一条进度明显不为 0），最终文件完整、能正常打开。

结果：[✅] 通过　问题：＿＿＿

### C3 相册批次恢复

操作：发一组相册下载中重启。

预期：
- 出现新消息「🔄 恢复文件夹任务 `文件夹名`，下载进度会在这条消息里更新。」，消息顶部带对批次第一条源媒体消息的引用，后续进度都更新在这条消息里；
- 旧的批次进度消息被改为「文件夹 `文件夹名` 的任务已中断，机器人重启后重新排队，请看新的进度消息。」。

结果：[✅] 通过　问题：＿＿＿

### C4 空队列重启

操作：确认没有任务（`/queue` 显示没有任务），重启。

预期：上线后不出现任何「恢复」消息；日志里也没有「发现上次未完成的下载任务」。

结果：[✅] 通过　问题：＿＿＿

### C5 边界：源消息已删（可选）

操作：入队一个任务后把 Telegram 里的源消息删掉，再重启。

预期：该任务被跳过（日志「恢复跳过：源消息不存在或已删除」），并顺带清理它留下的 `.temp` 断点残留（日志「已删除断点临时文件」）；若是批次则批次消息显示「文件夹 `文件夹名` 没有可恢复的任务（源消息可能已删除）。」。注意：目标文件已完整下载过的（日志「恢复跳过：文件已存在」）不会清理，成品保留。

结果：[✅] 通过　问题：＿＿＿

### C6 失败长周期自动重试

操作：让大文件下载中，断网（或关代理）3 分钟以上，观察日志和进度消息；恢复网络后继续观察。

预期：
- 约 2 分钟内日志出现「下载 xxx 短周期重试耗尽（……），5 分钟后进行第 1/4 次自动重试」，进度消息变为「⏳ 第 1/4 次自动重试将在 5 分钟 后开始，已下载 xx，断点已保留。」，**不出现**「下载失败」；
- 等待期间 `/queue` 里该任务显示「⏳ …第 1/4 次自动重试等待中」，`config/queue.json` 里任务仍在；
- 恢复网络后到点自动重新排队（日志「进入第 1 次自动重试」），从断点附近继续下载直至完成；
- 若等满 4 轮（5/15/30/60 分钟）网络一直不通，任务转入**冷驻留**：进度消息变「❄️ 4 次自动重试未成功，已保留断点、暂停重试；连接恢复后会自动继续。」，`/queue` 显示「❄️ …等待网络恢复」，`.temp` 与 `queue.json` 记录都保留，**不会**显示「下载失败」；
- 冷驻留状态下恢复网络，1 分钟内（会话健康检查通过）自动唤醒：日志「连接恢复，唤醒 N 个冷驻留任务继续下载」，从断点附近继续下载直至完成——天级断网（如代理挂两天）恢复后同样自动续上；
- 等待重试或冷驻留期间点进度消息「停止」或 `/queue` 取消：立即「已停止并删除」，记录和 `.temp` 一并清掉，之后不再自动重试；
- 冷驻留期间源消息被删除：下次唤醒时正式判死「下载失败：源消息已删除」，清断点清记录。

结果：[ ] 通过　问题：＿＿＿

### C7 磁盘写满（可选）

操作：把下载目录所在盘填到只剩很少空间（或临时调小容器磁盘限额），下载大文件触发 ENOSPC；然后清理空间并发 `/resume`。

预期：
- 日志出现「磁盘已满：xxx 已下载 xx，保留断点并暂停队列」，管理员收到「💾 磁盘已满…清理空间后发 /resume 继续。」通知（多条任务同时撞满只发一条）；
- 任务进度消息显示「💾 磁盘已满，断点已保留、队列已暂停…」，`.temp` 保留、`queue.json` 记录保留；
- `/resume` 回复带出「磁盘满驻留的 1 个任务已重新排队」，任务从断点附近继续下载；
- 空间没清理就 `/resume`：任务再次驻留并再发通知，断点不丢。

结果：[ ] 通过　问题：＿＿＿

---

## D. 频道监听（/listen）

### D1 开启 / 查看监听

操作：`/listen <频道链接>`（直接粘贴「复制频道链接」得到的链接，如 `https://t.me/xxx`；私有频道可用 `t.me/c/<id>/<msg>` 消息链接或邀请链接，`@用户名` 也认），然后看 `data/` 和 `/listening`；再试错误输入：`/listen`（不带参数）、`/listen https://t.me/不存在的频道`、对群组用 `/listen`。

预期：
- 成功（公开、私有一样）：回复「已开始监听 `频道标题`：新帖子自动下载到文件夹 `文件夹名/`。📜 历史回填：正在下载频道全部历史。」；`data/` 下出现同名文件夹；
- `/listening`：文本列表 + 每个频道一行按钮——频道名按钮（点击跳转到频道）和「🚫 取消监听」按钮（点击即移除该频道并刷新面板）；
- 无参数：显示用法提示；
- 频道不存在或用户账号未加入：回复「找不到这个频道，或用户账号未加入。」；
- 用户账号没加入该频道：回复「用户账号未加入该频道。」；
- 未配置用户账号：回复「需先配置用户账号（PHONE_NUMBER）。」；
- 群组：回复「群组暂不支持。」

结果：[ ] 通过　问题：＿＿＿

### D2 正常帖子下载

操作：频道发一个 50~200MiB 的视频。

预期：
- 频道里机器人**不**回复任何消息；
- 管理员私聊出现摘要消息，标题「📡 频道标题（自动下载）」；
- 文件下载到 `data/频道标题/`；
- 完成后摘要追加一行「✅ 文件名（大小）完成」；
- 下载过程中的临时进度消息约 10 秒后自动删除。

结果：[ ] 通过　问题：＿＿＿

### D3 相册

操作：频道一次发 3 张图。

预期：图片下载到 `data/频道标题/相册_x/` 子文件夹；摘要里每个文件一行 ✅。

结果：[ ] 通过　问题：＿＿＿

### D4 广告过滤

依次在频道发下面三条帖子，全部应被跳过、不产生下载，且摘要里给出跳过原因：

| 帖子文案 | 预期摘要原因 |
| :--- | :--- |
| 一张图，文案：`今日更新 #广告` | `⏭️ 跳过疑似广告（带广告标记）` |
| 一张图，文案：`进群看更多 https://t.me/+AbCdEf123` | `⏭️ 跳过疑似广告（含邀请链接和推广词）` |
| 一张图，文案：`内部群 兼职 招募` | `⏭️ 跳过疑似广告（推广词命中 3 个）` |

默认词库见 `config/listening.json` 的 `ad_keywords`（推广/广告/加微/微信/vx/…/进群/内部群/签约/招募）。可自己加词后重启再测。

结果：[ ] 通过　问题：＿＿＿

### D5 误杀检查

操作：频道发一条正常内容帖，例如纯图 + 文案「清晨的海边 随手拍」。

预期：正常下载，摘要出现 ✅，不被过滤。（普通文案里偶尔出现 1~2 个词库词也不应被过滤——过滤要求 ≥3 个词命中。）

结果：[ ] 通过　问题：＿＿＿

### D6 重复文件过滤

操作：把 D2 已下载的文件再发一次到频道。

预期：摘要显示「⏭️ 文件名：重复文件」，磁盘上没有重复下载。

结果：[ ] 通过　问题：＿＿＿

### D7 停止监听（/listening 面板）

操作：发 `/listening`，点目标频道行的「🚫 取消监听」；之后在频道再发帖；再发一次 `/listening` 确认。

预期：
- 点击弹「已停止监听：频道标题」，面板即时刷新（该行消失）；
- 之后发帖不下载、无摘要；
- 再发 `/listening`，被移除的频道不在列表里；
- 老版本监听的频道第一次打开面板时，链接会自动补上（日志「已补存频道链接」），之后频道名按钮可点击跳转。

结果：[ ] 通过　问题：＿＿＿

### D8 超大文件（可选）

操作：频道发一个 >2GiB 的文件。

预期：直接由用户账号下载（2GiB 级别无压力），正常完成并出现在摘要里。

结果：[ ] 通过　问题：＿＿＿

### D9 私有频道（机器人完全不在场）

操作：确认机器人**不在**该私有频道里、用户账号已加入、部署配置了 `PHONE_NUMBER`。`/listen <私有频道消息链接或邀请链接>`，然后在频道发帖（包含一个 >20MiB 的视频）。

预期：
- `/listen` 回复「已开始监听 …📜 历史回填：正在下载频道全部历史。」；
- 新帖自动下载到 `data/频道标题/`，摘要照常汇总到管理员会话；大文件直接走用户账号（不受 20MiB 限制）；
- 在 `/listening` 面板点该频道的「🚫 取消监听」能正常停止（提示「已停止监听」，面板即时刷新）。

结果：[ ] 通过　问题：＿＿＿

### D10 历史回填

操作：`/listen <频道链接> 100`（指定回填最近 100 条消息），观察管理员私聊摘要和 `data/频道标题/`；再发一次 `/listen` 同一链接（重复回填场景）；再用带 `#广告` 文案的历史帖测过滤。

预期：
- `/listen` 回复带「📜 历史回填：正在下载最近 100 条消息。」；
- 摘要出现「📜 开始回填历史：N 条媒体帖」，历史里的媒体按**从旧到新**的顺序依次入队下载，完成后摘要记「📜 历史回填完成」；
- 已下载过的历史文件自动跳过（摘要「⏭️ …：重复文件」）；带广告的历史帖同样被过滤；
- 不带条数参数时默认回填**全部历史**（`listening.json` 的 `history_limit` 可改：0=全部，-1=关闭，N=最近 N 条）。

结果：[✅] 通过　问题：＿＿＿

---

## E. 下载完成校验

### E1 正常完成

操作：本文件的下载用例走完后，抽查几个产物。

预期：所有「下载完成」的文件大小正常、能打开（隐含校验通过）。

结果：[✅] 通过　问题：＿＿＿

### E2 大小不符的表现（难人为触发，记录即可）

预期：一旦出现大小不符，文件会被删除，进度消息显示「文件 `xxx` 下载失败：大小不符（预期 X，实际 Y），已删除」；频道场景摘要显示「❌ 文件名 下载失败（大小不符…）」。

记录：本次测试是否出现过误杀：＿＿＿

---

## F. 自愈与限流退避（观察项，遇到才测）

### F1 会话假死自愈

现象：机器人再次「已读不回」。

预期：等约 3~5 分钟，日志出现「Telegram 会话疑似假死，正在重启连接（进程和下载队列保持不动）...」，之后自动恢复，无需手动重启。

结果：[ ] 触发过且自愈　[ ] 未触发　问题：＿＿＿

### F2 限流退避

操作：大量任务同时下载时观察日志。

预期：
- 不应刷屏「更新下载消息失败：FloodWait」；
- 真触发限流时日志出现「编辑消息触发限流 N 秒，期间暂停该会话的进度刷新」，冷却期内该会话进度编辑暂停（最终状态消息照发）。

结果：[ ] 通过　[ ] 未触发　问题：＿＿＿

---

## G. 回归（老功能抽查）

### G1 私聊发文件（核心链路）

操作：私聊发一个大文件，观察全程；再各用一种方式改名：回复进度消息发新名字 / 文件说明里写名字 / 转发附带名字。

预期：
- 进度消息实时更新百分比、已下/总大小、速度、预计剩余时间，带「停止」按钮；
- 完成消息「文件 `xxx` 已下载完成。共 X，用时 Y，平均速度 Z/s」；
- 改名生效（不用写后缀，自动补）；一次发多个文件存进同一个文件夹，改名只改文件夹名。

结果：[ ] 通过　问题：＿＿＿

### G2 重复文件提示（普通会话）

操作：把 G1 已下载的文件原样再发一次；同一文件下载完成后再发一次不同名的相同内容。

预期：
- 第二次入队前弹「这个文件下载过，取消还是继续？」（按钮：取消下载 / 继续下载）；选「继续下载」能正常下；
- 内容相同（哈希一致）时完成后弹「和历史下载的文件内容相同，保留还是删除？」（按钮：保留 / 删除），两个选择都能正确收尾。

结果：[ ] 通过　问题：＿＿＿

### G3 受限频道与目录命令

操作：`/add 消息链接`（私有受限频道）、`/use test/sub`、`/get`、`/leave`、`/usage`。

预期：
- `/add` 正常下载受限频道文件（需已配置用户账号）；格式错误时回复链接格式提示；
- `/use` 后新文件进子目录（`../` 会被清理并提示实际目录）；`/get` 显示当前目录；`/leave` 回复「已经回到根目录了。」；`/usage` 显示总容量/已用/剩余。

结果：[ ] 通过　问题：＿＿＿

### G4 /help

操作：发 `/help`。

预期：列出全部命令，包含新命令 `/queue`、`/pause`、`/resume`、`/files`、`/listen`、`/listening`（`/unlisten` 已被面板按钮取代，不应出现在列表里）。

结果：[✅] 通过　问题：＿＿＿

### G5 版本更新提示

操作：把 `config/last_version` 的内容改成一个旧版本号（如 `1.0.0`）→ 重启；再重启一次；最后把该文件删掉再重启。

预期：
- 第一次重启：管理员会话收到「🆕 TDownloader 已更新：1.0.0 → 当前版本」，且 `last_version` 内容被更新为当前版本；
- 第二次重启：不再提示；
- 文件删掉后重启：首次运行，只记录版本号、不发消息（日志「首次运行，记录版本号」）。

结果：[✅] 通过　问题：＿＿＿

---

## 已知行为说明（不是 bug）

- 收帖与下载统一走用户账号：机器人无需加入任何频道，`/listen` 时会探测用户账号是否已加入；未配置用户账号则无法监听。
- 频道监听只支持频道（channel），群组暂不支持。
- 频道下载的临时进度消息约 10 秒后自动删除，结果以管理员会话的频道摘要为准。
- `/pause` 状态不跨重启保留，重启后自动恢复下载。
- 手动「停止」会删除已下载部分（含 `.temp` 断点），属有意行为，不提供恢复；断点续传只覆盖自动中断（进程内失败、重启恢复、长周期重试）。
- 等待自动重试/磁盘恢复的驻留任务不出现在普通排队列表里，`/queue` 会单独列出，可正常取消。
- 默认并发 6，任务数 ≤ 6 时看不到排队状态；测排队要把 `MAX_CONCURRENT_DOWNLOADS` 调成 1。

## 修复与变更记录（2026-09-12，依据测试结果）

- **新增（跳转）：/queue 每行新增「📍 进度」按钮，点一下定位到该任务的进度消息。**
  Telegram 私聊消息没有链接、无法直接跳转，所以实现为：点「📍 进度」→ 机器人在该任务的进度消息下面回一条带引用的定位消息（15 秒后自动删除）→ 点定位消息顶部的引用即滚动到进度消息。`⏹` 取消按钮保持原样，进度消息自身的按钮也不动。
- **新增（引用）：重启恢复的提示消息带上对原媒体消息的引用。**
  C1 的「🔄 恢复下载任务」引用自己的源媒体消息，C3 的「🔄 恢复文件夹任务」引用批次第一条源媒体消息，点引用即可跳回原消息。频道监听任务的源消息在频道里，不跨会话引用（保持纯文本）；引用发送失败自动退回纯文本，恢复流程不受影响。
- **修复（C5 反馈）：恢复被跳过的任务现在会顺带清理它留下的 `.temp` 断点残留。**
  之前源消息已删等跳过场景只从 queue.json 移除任务，`.temp` 一直躺在磁盘上；现在跳过时自动删除（日志「已删除断点临时文件」）。目标文件已完整下载的仍保留不动。
- **新增（G5）：程序版本更新后主动提示管理员。**
  启动时对比 `config/last_version`（上次运行的版本号）和当前版本，不一致就给管理员会话发「🆕 TDownloader 已更新：旧 → 新」。首次运行只记录版本不发消息；发送失败或管理员会话不可用时不记录新版本，下次启动重试。
- **修复（重要，D 组测试前必测）：/listen 一旦解析成功会卡死整个机器人。**
  根因：listen() 持有 `_lock` 期间调用 save_config()，save_config 又去拿同一把不可重入锁，同线程死锁——事件循环被冻结，所有功能无响应。已把 save_config() 移到锁外。这是 D1 一直没测到才没暴露的。
- **改进（D1 反馈）：/listen、/unlisten 直接认频道链接，不再要求 @用户名。**
  公开频道「复制频道链接」得到的 `https://t.me/xxx`、私有频道的 `t.me/c/<id>/<msg>` 消息链接（转成数字 id 解析，带网络兜底）、邀请链接 `t.me/+xxx`、`@用户名`、裸用户名都可以。文档和命令提示同步更新。
- **修复（D9 实测）：邀请链接监听的两个错误。**
  ① `messages.CheckChatInvite` 是用户专属方法，机器人解析 `t.me/+xxx` 邀请链接必报 BOT_METHOD_INVALID——现在邀请链接直接走用户账号解析，不让机器人白试一次；② 这个 Pyrogram fork 的 `get_chat_history` 是异步生成器，加入频道探测改用 `async for` 迭代（之前 `await` 直接抛 TypeError，把已在频道里的用户误报成「还没加入」）。
- **新增（D10，需求）：/listen 支持历史回填。**
  监听建立后自动下载频道的历史媒体帖，从旧到新按顺序下载；**不填条数默认回填全部历史**，命令第二参数可只回填最近 N 条，`listening.json` 的 `history_limit` 全局设置（0=全部，-1=关闭，N=最近 N 条）。回填与实时新帖共用同一套过滤（类型/大小/广告）和去重，已下载过的自动跳过；回填统一走用户账号；中途 `/unlisten` 会中止回填；摘要记录「📜 开始回填历史 / 历史回填完成」。
- **新增（需求）：/listening 面板化——每个频道一行按钮。**
  左侧按钮为频道名，点击直接跳转到频道（公开频道用用户名链接、私有频道用 /listen 时保存的邀请链接，`/listen` 时存进 listening.json）；右侧「🚫 取消监听」点击即移除该频道并刷新面板；全部取消后面板显示空列表提示。**`/unlisten` 命令已移除**，由面板按钮取代；旧版监听条目缺链接的，打开面板时会自动补存（日志「已补存频道链接」）。
- **修复（需求实测）：普通成员的私有频道存不了跳转链接。**
  导出邀请链接是管理员权限，普通成员 `get_chat` 拿不到 invite_link，link 只能存空。修复：用户**用邀请链接来监听时直接把该链接存下来**（规范化补 https://）；另外修复 t.me/c/ 链接转数字 id 后误做邀请链接匹配导致的 TypeError。普通成员私有频道若不是用邀请链接监听的，仍无跳转按钮（重新用邀请链接 /listen 一次即可补上）。
- **简化（需求确认）：监听的唯一要求 = 用户账号加入频道，机器人彻底退出收帖。**
  公开、私有统一：/listen 一律用用户账号解析并探测成员资格（机器人解析步骤删除）；收帖处理器只挂用户账号，机器人侧只保留一个吞帖器（防止 addFile 把频道帖当私聊文件下载、往频道回消息）；频道媒体全部由用户账号直接下载，via 字段和 20MiB 分支删除。未配置用户账号时 /listen 直接提示需要配置。
- **修复（D9 实测，根因）：用户账号客户端带 `no_updates=True`，收不到任何更新。**
  旧架构遗留：用户账号只当下载工人时这个参数是合理的。但 via 方案里私有频道的收帖完全依赖用户账号，这个开关让「监听成功却永远不触发」——会话连着，服务端却一个更新都不推。已去掉该参数，用户账号现在正常接收频道新帖。
- **新增（需求）：/start 改为按钮菜单。**
  欢迎语下带 5 个按钮（两列布局）：📋 下载队列、📂 文件管理、📡 监听频道、💾 磁盘空间、❓ 帮助——点击后机器人回复对应页面（管理员校验按点击者判断，非管理员弹「你不是管理员」）。
- **新增（需求）：/queue 增加「🧹 全部取消」。**
  一键停止并清空所有任务：排队中的立即收尾（消息变「已停止并删除」、移出队列和落盘），下载中的标记停止（传输停止后收尾），批次走批次清理（文件删除、消息收尾）；面板即时刷新为空。
- **新增（需求）：/files 支持删除整个目录。**
  进入子目录后导航行多出「🗑 删除此目录」：确认后整个目录（含全部内容）删除、回到上级列表，去重记录同步清理；目录内有正在下载的文件（.temp）时，自动取消占用它的下载任务并中止该频道的历史回填，然后照常删除（确保一次删净，不残留重建）；根目录没有该按钮。
- **精简（反馈）：机器人提示类回复全面瘦身。**
  /listen 用法与成功尾巴、报错从句、进度定位消息、相册提示、/add 报错、/use 确认——只保留「什么问题 + 该做什么」，使用细节移入 /help 和文档；`/use` 成功回复现在直接带出新路径。
- **回退（架构决定）：「机器人 = 交互层，用户账号 = 数据面」的重构已回退，恢复为按频道分工（via）的方案。**
  重构期间实测学到两个关键机制：① file_reference 按获取消息的账号上下文发放，机器人手里的引用用户账号用不了（FILE_REFERENCE_EXPIRED），跨会话下载必须以用户视角（私聊对端 = 机器人自己的 id）重取；② 用户账号会话要先按用户名解析一次机器人，否则按 id 取消息报 PeerIdInvalid。这些经验保留在变更记录里，完整重构代码存放在 git stash（「架构重构（机器人=交互层/用户账号=数据面）」），以后了解机制更深了可以再捡起来。
- **新增（反馈）：删除文件时同步清理去重数据库记录。**
  /files 删除文件、批次停止清理文件后，去重数据库（`config/downloads.sqlite`）里对应记录一并删除——否则下次发同一个文件会被误判「下载过」或「和历史内容相同」。
- **勘误：机器人 20MiB 下载上限的说法是错的。**
  20MiB 是 Bot API（HTTP 接口）的限制；本项目机器人走 MTProto 协议，实测可下载远超 20MiB 的文件。因此「超过 20MiB 转用户账号」更多是稳妥策略而非硬需求；超大文件（>2GiB）仍需用户账号。

- **A3 反馈：排队任务取消后「已停止并删除」延迟大、和别的任务耦合。**
  修复：取消还没开始传输的任务时立即收尾——消息马上改成「已停止并删除」，任务从队列和 `queue.json` 移除，不再等调度器出队。复测 A3，确认消息秒改。
- **A4 反馈 1：批次停止后已完成的文件没被删。**
  修复：清理逻辑重写——文件夹里每个条目都尝试删除（子目录整棵递归删），不再用 `is_file()` 静默跳过；之前那种「删 0 个、剩 1 个、目录删不掉」的路径不会再静默漏删。
- **A4 反馈 2：日志报 `TypeError: 'module' object is not callable`（cleanup.py:59）。**
  修复：`time()` 误用模块当函数，改为 `time.time()`。之前后台重试任务一启动就崩、残留永远不重试；现在句柄占用等暂时删不掉的条目会在几秒内被后台重试补删。复测 A4，确认停止后整个文件夹（含已完成文件）消失。
- **A4 复测遗留：队列里最后一个任务（批次）取消后，/queue 面板没及时刷新。**
  修复：/queue 里点批次「停止」时先打上停止标记再刷新面板——之前停止标记在后台任务里才置位，刷新那一帧仍会把该批次画出来，看起来像没刷新。复测：点停止后面板应立即少一行。
- **新发现（双层目录）：相册被下载进 `data/文件夹名/文件夹名/` 两层同名目录。**
  根因：362f8f7 提交改入队相对路径时把子目录拼了两次；这也正是此前 A4 日志里「删除 0 个文件、剩余 1 个」的原因——剩下的 1 个条目就是套在里面的同名内层文件夹。修复后私聊相册、/use 子目录相册、频道相册都恢复单层。复测：发一组相册，`data/` 下应只有一层 `文件夹名/`，文件直接在里面。之前双层 bug 留下的旧残留目录（内层同名文件夹）需手动删一次。

## 修复与变更记录（2026-09-13，断点续传强化）

- **新增（C6）：短周期重试耗尽不再丢断点，转入长周期自动重试。**
  之前断网约 2 分钟（进程内 6 次退避耗尽）或限流超过 10 次，任务直接失败、`.temp` 断点被删、记录从 `queue.json` 移除——已下载的部分全部作废。现在耗尽后保留断点并驻留，按 5/15/30/60 分钟递增间隔自动重排（最多 4 轮），期间记录保留在 `queue.json`，进程重启照常恢复续传；轮次用尽或源消息已删除才终态失败并清断点。等待期间进度消息显示「⏳ 第 N/4 次自动重试…」、`/queue` 单独列出（可正常取消），批次条目回到等待状态、批次不会提前收尾。测试见 C6。
- **新增（C7）：磁盘写满自护。**
  写盘遇到 ENOSPC 不再白白烧完重试次数：任务驻留（断点、记录都保留）、自动暂停出队、通知管理员（60 秒节流防连发）；清理空间后 `/resume` 释放驻留任务从断点继续，回复带出释放数量。测试见 C7。
- **加固：损坏断点归零重下。**
  `.temp` 比源文件还大（异常残留）时不再被当成「已下载完」直接替换成品，先归零重新下载；堵住 `expected_size` 未知时坏断点漏过完成校验的口子。
- **明确（需求决定）：手动停止不提供恢复。**
  所有手动停止入口（进度消息「停止」、`/queue` 单任务取消/全部取消、批次「停止全部」、`/files` 删除占用目录）维持「终止传输 + 删除成品与 `.temp`」，之前挂在「以后可做」的「停止后保留 .temp」从规划中移除，不再规划。README 特性与已知行为说明已同步。

### 断网两天场景：长重试轮次用尽转入冷驻留（2026-09-13 追加）

- **新增：轮次用尽不再终态失败，转入冷驻留等连接恢复。**
  之前 4 轮长重试（约 2 小时）烧完就判死：删断点、删记录，天级断网（如代理挂两天）恢复后只能重发文件从头下。现在轮次用尽转入冷驻留——`.temp` 与 `queue.json` 记录一直保留，进度消息变「❄️ …连接恢复后会自动继续」，批次条目显示「等待网络恢复」；`run.py` 会话健康检查（60 秒一次）一通过就唤醒全部冷驻留任务，重取源消息确认还在后重新排队续传。唤醒后的一次尝试再失败会直接回到冷驻留（每轮健康检查最多唤醒一次）。测试见 C6。
- **顺带：源消息已删的检测提前到每次重试/唤醒时。**
  长重试唤醒与冷驻留唤醒都会重取源消息：调用成功但返回为空即判死「下载失败：源消息已删除」（清断点清记录），避免已删源的任务无限空转。
- **边界：冷驻留任务跨进程重启 = 重新获得一轮完整重试周期。**
  冷驻留的任务记录在 `queue.json`，重启恢复后立即重试（retry_round 不跨重启保留）——网络恢复时的重启等效于直接唤醒。

## 问题记录

| 用例编号 | 现象（截图/文案） | 日志关键行 | 备注 |
| :--- | :--- | :--- | :--- |
|  |  |  |  |
