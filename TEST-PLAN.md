# 测试计划（2026-09-12 晨）

> 分支 `dev/20260912-features`，每个用例对应一个功能点，测完在前面打勾。
> 测试前置：重启机器人（面板「停止 → 启动」），让分支代码生效。

## A. 队列管理（/queue、/pause、/resume）

- [ ] **A1 队列视图**：同时发 3 个以上大文件，发 `/queue` → 应显示「进行中 ⬇️ / 排队中 ⏳」分组、文件名和大小，每个任务下有「⏹ 取消」按钮，底部有「🔄 刷新」。
- [ ] **A2 取消进行中**：点一个正在下载任务的「⏹ 取消」→ 该任务从 /queue 列表消失，原进度消息变为「已停止并删除」，磁盘上无 `.temp` 残留。
- [ ] **A3 取消排队中**：点一个排队任务的「⏹ 取消」→ 一秒内从列表消失，原「已加入下载队列」消息变为「已停止并删除」。
- [ ] **A4 文件夹任务**：发一组相册后 `/queue` → 显示 `📁 相册名（完成 x/y）`，点「⏹ 停止」→ 批次消息变「已停止并删除」，文件夹整个清掉。
- [ ] **A5 暂停恢复**：排队若干任务后发 `/pause` → 新任务不再开始（进行中的会下完），`/queue` 头部显示「⏸ 已暂停」；发 `/resume` → 排队任务开始下载。

## B. 文件管理（/files）

- [ ] **B1 浏览**：`/files` → 显示当前目录 `/` 与条目统计；点 `📁` 进入子目录，`⬅️ 上一级` 返回，`🔄 刷新` 更新。
- [ ] **B2 删除文件**：点 `📄 文件` → 显示大小/修改时间详情 → 点「🗑 删除此文件」→ 出现「⚠️ 确认删除」→ 点「✅ 确认删除」→ 文件从磁盘消失，列表刷新；「❌ 取消」则不删。
- [ ] **B3 正在下载的文件**：对一个正在下载中的文件（`.temp`）尝试删除 → 应提示「这个文件正在下载中，请先取消下载。」且文件未被删。

## C. 队列持久化（重启恢复）

- [ ] **C1 入队后重启**：排 3 个任务（含一个正在下载、两个排队）→ 面板「停止」→「启动」→ 机器人上线后应出现「🔄 恢复下载任务/恢复文件夹任务」的新消息，任务继续下载；`config/queue.json` 里的任务随之清空。
- [ ] **C2 断点续传**：重启前让大文件下载过半 → 重启后恢复 → 该文件从 `.temp` 断点继续（进度起点不为 0，最终能完整下载打开）。
- [ ] **C3 相册批次恢复**：发一组相册下载中重启 → 恢复后出现新批次进度消息，旧批次消息被标注「已中断…重新排队」。
- [ ] **C4 空队列重启**：无任务时重启 → 不应出现任何恢复消息。

## D. 频道监听（/listen）

- [ ] **D0 前置**：把机器人加进测试频道（管理员）；用户账号（手机号登录的）也加入该频道（下载 >20MiB 文件必需）。
- [ ] **D1 开启监听**：`/listen @频道用户名` → 回复「已开始监听」，`data/` 下出现 `频道标题/` 文件夹；`/listening` 能看到该频道。
- [ ] **D2 正常帖子下载**：频道发一个视频（约 50~200MiB）→ 机器人不回复频道；管理员会话出现该频道的「📡 摘要消息」，文件下载到 `data/频道标题/`，完成后摘要里出现 `✅ 文件名（大小）完成`，临时进度消息约 10 秒后自动删除。
- [ ] **D3 相册**：频道一次发 3 张图（相册）→ 下载到 `data/频道标题/相册_x/`，摘要里每个文件一行 ✅。
- [ ] **D4 广告过滤**：依次发这些帖子，全部应被跳过且摘要给出原因、不产生下载：
  - 文案带 `#广告` 的图片
  - 文案 `进群看更多 https://t.me/+xxxx`（邀请链接 + 推广词）
  - 文案含 3 个以上推广词（如 `内部群 兼职 招募`）
- [ ] **D5 误杀检查**：普通内容帖（如「清晨的海边 随手拍」纯图无文案）→ 正常下载，不被过滤。
- [ ] **D6 重复过滤**：把 D2 已下载的文件再发一次到频道 → 摘要显示「⏭️ …：重复文件」，不重复下载。
- [ ] **D7 停止监听**：`/unlisten @频道用户名` → 之后再发帖不下载。
- [ ] **D8 大文件（可选）**：发一个 >2GiB 的文件 → 走用户账号下载（日志可见 user client），正常完成。

## E. 下载完成校验

- [ ] **E1 正常**：正常下载完成的文件都能打开（隐含校验通过）。
- [ ] **E2 失败表现**：若出现大小不符（罕见），文件会被删除并在进度消息提示「下载失败：大小不符（预期 …，实际 …）」。记录是否出现过误杀。

## F. 自愈与限流退避（观察项）

- [ ] **F1 会话假死自愈**：若机器人再次出现"已读不回"，等约 3~5 分钟 → 日志应出现「Telegram 会话疑似假死，正在重启连接」并自动恢复，无需手动重启。
- [ ] **F2 限流退避**：大量任务同时下载时，日志不应再刷屏 `更新下载消息失败：FloodWait`；出现一次限流后会暂停进度编辑到冷却结束。

## G. 回归（老功能抽查）

- [ ] **G1**：普通私聊发文件 → 下载、改名（回复进度消息发新名字）、相册建文件夹，均与之前一致。
- [ ] **G2**：`/add 链接` 受限频道下载、`/use`、`/get`、`/leave`、`/usage` 正常。
- [ ] **G3**：`/help` 能看到全部新命令说明。

## 已知行为说明（非 bug）

- 频道监听只支持频道（channel），群组暂不支持。
- 频道下载的进度消息在管理员会话展示约 10 秒后自动删除，最终结果以频道摘要为准。
- `/pause` 状态不跨重启保留（重启后自动恢复下载）。
- 手动「停止」仍然会删除已下载部分（保留 `.temp` 待做）。
